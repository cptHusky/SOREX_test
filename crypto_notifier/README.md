# SOREX_test
```
usage: run.py [-h] [--log-level LOG_LEVEL] [--log-path LOG_PATH]

Run the crypto notifier bot.

options:
  -h, --help            show help message and exit
  --log-level LOG_LEVEL
                        Set the logging level (e.g., DEBUG, INFO, WARNING,
                        ERROR, CRITICAL)
  --log-path LOG_PATH   Path to log file
